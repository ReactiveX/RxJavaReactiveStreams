rootProject.name = 'rxjava-reactive-streams'

include "rxjava-reactive-streams"

if (System.properties.containsKey("enableExamples")) {
    if (JavaVersion.current().java8Compatible) {
        include "examples:ratpack"
    }
}